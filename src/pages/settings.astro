---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/common/Navbar.astro';
import { Palette, Check, Sun, Moon, Monitor } from 'lucide-astro';
---

<Layout title="Ajustes | KazeChronik">
  <Navbar />

  <main class="flex-1 max-w-xl w-full mx-auto px-4 py-8">
    <div class="mb-6">
      <h1 class="text-2xl font-bold theme-text-primary tracking-tight">Ajustes</h1>
      <p class="text-xs theme-text-secondary mt-0.5">Personaliza la apariencia y preferencias de tu cuenta</p>
    </div>

    <!-- Sección de Apariencia -->
    <div class="theme-card border rounded-2xl p-5 shadow-xl mb-6">
      <div class="flex items-center gap-3 mb-4 pb-3 border-b border-slate-500/20">
        <div class="p-2 bg-indigo-500/10 border border-indigo-500/20 rounded-xl text-indigo-500">
          <Palette class="w-4 h-4" />
        </div>
        <div>
          <h2 class="text-sm font-bold theme-text-primary">Tema Visual</h2>
          <p class="text-[11px] theme-text-secondary">Selecciona el esquema de colores de la interfaz</p>
        </div>
      </div>

      <!-- Retícula Horizontal Compacta -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
        <!-- Legacy Dark -->
        <button
          type="button"
          data-theme="legacy"
          class="theme-opt p-3 rounded-xl border theme-card text-left transition-all relative flex flex-col justify-between h-24 cursor-pointer hover:border-indigo-500/50"
        >
          <div class="flex items-center justify-between">
            <Moon class="w-4 h-4 theme-text-secondary" />
            <span class="check-icon hidden text-indigo-500"><Check class="w-3.5 h-3.5" /></span>
          </div>
          <div>
            <span class="block text-xs font-bold theme-text-primary">Legacy Dark</span>
            <span class="block text-[10px] theme-text-secondary">Azul oscuro</span>
          </div>
        </button>

        <!-- Midnight Black -->
        <button
          type="button"
          data-theme="dark"
          class="theme-opt p-3 rounded-xl border theme-card text-left transition-all relative flex flex-col justify-between h-24 cursor-pointer hover:border-indigo-500/50"
        >
          <div class="flex items-center justify-between">
            <Monitor class="w-4 h-4 theme-text-secondary" />
            <span class="check-icon hidden text-indigo-500"><Check class="w-3.5 h-3.5" /></span>
          </div>
          <div>
            <span class="block text-xs font-bold theme-text-primary">Midnight</span>
            <span class="block text-[10px] theme-text-secondary">Oscuro puro</span>
          </div>
        </button>

        <!-- Modo Claro -->
        <button
          type="button"
          data-theme="light"
          class="theme-opt p-3 rounded-xl border theme-card text-left transition-all relative flex flex-col justify-between h-24 cursor-pointer hover:border-indigo-500/50"
        >
          <div class="flex items-center justify-between">
            <Sun class="w-4 h-4 theme-text-secondary" />
            <span class="check-icon hidden text-indigo-500"><Check class="w-3.5 h-3.5" /></span>
          </div>
          <div>
            <span class="block text-xs font-bold theme-text-primary">Modo Claro</span>
            <span class="block text-[10px] theme-text-secondary">Luminoso</span>
          </div>
        </button>
      </div>
    </div>

    <!-- Formulario de Perfil Público -->
    <section class="theme-card border p-6 rounded-2xl space-y-4 shadow-sm mb-6">
      <h2 class="font-bold text-lg border-b border-slate-500/20 pb-3 theme-text-primary">👤 Perfil Público</h2>
      
      <form id="profile-form" class="space-y-4">
        <div>
          <label class="block text-xs font-semibold theme-text-secondary mb-1">Nombre de Usuario (@username)</label>
          <input 
            type="text" 
            id="profile-username" 
            readonly 
            disabled
            class="w-full px-4 py-2.5 theme-input border rounded-xl text-sm font-bold theme-text-secondary opacity-60 cursor-not-allowed"
          />
          <p class="text-[10px] theme-text-secondary mt-1">El nombre de usuario es tu identificador único e inmodificable.</p>
        </div>

        <div>
          <label class="block text-xs font-semibold theme-text-secondary mb-1">Nombre Visible</label>
          <input 
            type="text" 
            id="profile-display-name" 
            placeholder="ej: Ángel González"
            class="w-full px-4 py-2.5 theme-input border rounded-xl text-sm focus:outline-none focus:border-indigo-500"
          />
        </div>

        <div>
          <label class="block text-xs font-semibold theme-text-secondary mb-1">Biografía</label>
          <textarea 
            id="profile-bio" 
            rows="3" 
            placeholder="Escribe algo sobre tus gustos en videojuegos..."
            class="w-full px-4 py-2.5 theme-input border rounded-xl text-sm focus:outline-none focus:border-indigo-500 resize-none"
          ></textarea>
        </div>

        <button 
          type="submit" 
          class="px-5 py-2.5 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold text-sm rounded-xl transition-all shadow-lg shadow-indigo-600/20 cursor-pointer"
        >
          Guardar Perfil
        </button>
      </form>
    </section>

    <!-- Copia de Seguridad & Datos -->
    <section class="theme-card border p-6 rounded-2xl space-y-4 shadow-sm mb-6">
      <h2 class="font-bold text-lg border-b border-slate-500/20 pb-3 flex items-center gap-2 theme-text-primary">
        📦 Copia de Seguridad & Datos
      </h2>
      <p class="text-xs theme-text-secondary">
        Exporta una copia de respaldo en formato JSON de toda tu biblioteca de juegos, horas y reseñas, o restaura tus datos desde un archivo previo.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 pt-2">
        <!-- Exportar -->
        <button 
          id="export-backup-btn"
          type="button"
          class="flex-1 px-4 py-2.5 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold text-xs rounded-xl transition-all shadow-lg shadow-indigo-600/20 cursor-pointer flex items-center justify-center gap-2"
        >
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/>
          </svg>
          <span>Exportar Biblioteca (JSON)</span>
        </button>

        <!-- Importar -->
        <label class="flex-1 px-4 py-2.5 theme-button hover:border-indigo-500/50 theme-text-primary font-semibold text-xs rounded-xl transition-all cursor-pointer flex items-center justify-center gap-2 border text-center">
          <svg class="w-4 h-4 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/>
          </svg>
          <span>Restaurar Copia (JSON)</span>
          <input type="file" id="import-backup-input" accept=".json" class="hidden" />
        </label>
      </div>
    </section>

    <!-- Sección de Seguridad & Contraseña -->
    <section class="theme-card border p-6 rounded-2xl space-y-4 shadow-sm mb-6">
      <h2 class="font-bold text-lg border-b border-slate-500/20 pb-3 flex items-center gap-2 theme-text-primary">
        🔒 Seguridad & Contraseña
      </h2>
      
      <form id="password-form" class="space-y-4">
        <div>
          <label class="block text-xs font-semibold theme-text-secondary mb-1">Nueva Contraseña</label>
          <input 
            type="password" 
            id="new-password" 
            required
            minlength="6"
            placeholder="••••••••"
            class="w-full px-4 py-2.5 theme-input border rounded-xl text-sm focus:outline-none focus:border-indigo-500"
          />
        </div>

        <div>
          <label class="block text-xs font-semibold theme-text-secondary mb-1">Confirmar Nueva Contraseña</label>
          <input 
            type="password" 
            id="confirm-password" 
            required
            minlength="6"
            placeholder="••••••••"
            class="w-full px-4 py-2.5 theme-input border rounded-xl text-sm focus:outline-none focus:border-indigo-500"
          />
        </div>

        <button 
          type="submit" 
          id="change-password-btn"
          class="px-5 py-2.5 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold text-sm rounded-xl transition-all shadow-lg shadow-indigo-600/20 cursor-pointer"
        >
          Actualizar Contraseña
        </button>
      </form>
    </section>
  </main>
</Layout>

<script>
  import { supabase } from '../lib/supabase';

  // Lógica del Conmutador de Temas Visuales
  function initThemeSelector() {
    const buttons = document.querySelectorAll<HTMLButtonElement>('.theme-opt');
    const currentTheme = localStorage.getItem('theme') || 'legacy';

    function applyActiveStyles(selectedBtn: HTMLButtonElement) {
      buttons.forEach(b => {
        b.classList.remove('border-2', 'border-indigo-500', 'ring-2', 'ring-indigo-500/20');
        b.classList.add('border');
        b.querySelector('.check-icon')?.classList.add('hidden');
      });

      selectedBtn.classList.remove('border');
      selectedBtn.classList.add('border-2', 'border-indigo-500', 'ring-2', 'ring-indigo-500/20');
      selectedBtn.querySelector('.check-icon')?.classList.remove('hidden');
    }

    const initialBtn = document.querySelector<HTMLButtonElement>(`.theme-opt[data-theme="${currentTheme}"]`);
    if (initialBtn) {
      applyActiveStyles(initialBtn);
    }

    buttons.forEach(btn => {
      btn.addEventListener('click', () => {
        applyActiveStyles(btn);
        const selectedTheme = btn.getAttribute('data-theme') || 'legacy';
        localStorage.setItem('theme', selectedTheme);
        document.documentElement.setAttribute('data-theme', selectedTheme);
      });
    });
  }

  async function initSettingsPage() {
    initThemeSelector();

    const { data: { session } } = await supabase.auth.getSession();
    if (!session) {
      window.location.href = '/login';
      return;
    }

    const usernameInput = document.getElementById('profile-username') as HTMLInputElement;
    const nameInput = document.getElementById('profile-display-name') as HTMLInputElement;
    const bioInput = document.getElementById('profile-bio') as HTMLTextAreaElement;
    const profileForm = document.getElementById('profile-form') as HTMLFormElement;
    const passwordForm = document.getElementById('password-form');
    const newPasswordInput = document.getElementById('new-password') as HTMLInputElement;
    const confirmPasswordInput = document.getElementById('confirm-password') as HTMLInputElement;
    const changePasswordBtn = document.getElementById('change-password-btn') as HTMLButtonElement;

    // 1. Cargar perfil actual desde Supabase
    const { data: profile } = await supabase
      .from('profiles')
      .select('*')
      .eq('id', session.user.id)
      .maybeSingle();

    if (profile) {
      if (usernameInput) usernameInput.value = profile.username || '';
      if (nameInput) nameInput.value = profile.display_name || '';
      if (bioInput) bioInput.value = profile.bio || '';
    }

    // 2. Listener para Guardar Perfil Público
    profileForm?.addEventListener('submit', async (e) => {
      e.preventDefault();

      const displayNameVal = nameInput.value.trim();
      const bioVal = bioInput.value.trim();

      const { error } = await supabase
        .from('profiles')
        .update({
          display_name: displayNameVal || usernameInput.value,
          bio: bioVal,
          updated_at: new Date().toISOString()
        })
        .eq('id', session.user.id);

      if (error) {
        alert('Error al actualizar el perfil: ' + error.message);
      } else {
        alert('¡Perfil actualizado con éxito!');
      }
    });

    passwordForm?.addEventListener('submit', async (e) => {
      e.preventDefault();

      const newPassword = newPasswordInput.value;
      const confirmPassword = confirmPasswordInput.value;

      if (newPassword !== confirmPassword) {
        alert('Las contraseñas no coinciden. Por favor verifica.');
        return;
      }

      changePasswordBtn.disabled = true;
      changePasswordBtn.textContent = 'Actualizando...';

      const { error } = await supabase.auth.updateUser({ password: newPassword });

      changePasswordBtn.disabled = false;
      changePasswordBtn.textContent = 'Actualizar Contraseña';

      if (error) {
        alert('Error al actualizar contraseña: ' + error.message);
      } else {
        alert('¡Tu contraseña ha sido actualizada con éxito!');
        newPasswordInput.value = '';
        confirmPasswordInput.value = '';
      }
    });

    // 3. Listener para Exportar Backup (JSON)
    const exportBtn = document.getElementById('export-backup-btn');
    exportBtn?.addEventListener('click', async () => {
      const { data: games, error } = await supabase
        .from('user_library')
        .select('*')
        .eq('user_id', session.user.id);

      if (error || !games) {
        alert('Error al obtener datos para la copia de seguridad.');
        return;
      }

      const backupData = {
        app: 'KazeChronik',
        version: '1.0',
        exported_at: new Date().toISOString(),
        total_games: games.length,
        library: games
      };

      const jsonStr = JSON.stringify(backupData, null, 2);
      const blob = new Blob([jsonStr], { type: 'application/json' });
      const url = URL.createObjectURL(blob);

      const a = document.createElement('a');
      a.href = url;
      a.download = `kazechronik-backup-${new Date().toISOString().slice(0, 10)}.json`;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
    });

    // 4. Listener para Importar Backup (JSON)
    const importInput = document.getElementById('import-backup-input') as HTMLInputElement;
    importInput?.addEventListener('change', async (e: any) => {
      const file = e.target.files?.[0];
      if (!file) return;

      const reader = new FileReader();
      reader.onload = async (event) => {
        try {
          const content = JSON.parse(event.target?.result as string);
          const gamesToImport = content.library || content;

          if (!Array.isArray(gamesToImport)) {
            alert('El archivo JSON no tiene un formato válido de biblioteca.');
            return;
          }

          if (!confirm(`Se van a importar ${gamesToImport.length} juegos a tu cuenta. ¿Deseas continuar?`)) {
            return;
          }

          const preparedGames = gamesToImport.map(g => ({
            user_id: session.user.id,
            title: g.title,
            cover_url: g.cover_url || null,
            platform: g.platform || 'PC',
            store: g.store || 'Otra',
            status: g.status || 'backlog',
            hours_played: Number(g.hours_played) || 0,
            rating: g.rating ? Number(g.rating) : null,
            review: g.review || null,
            notes: g.notes || null,
            tags: Array.isArray(g.tags) ? g.tags : [],
            is_favorite: Boolean(g.is_favorite),
            updated_at: new Date().toISOString()
          }));

          const { error } = await supabase
            .from('user_library')
            .insert(preparedGames);

          if (!error) {
            alert('¡Copia de seguridad restaurada con éxito!');
            window.location.href = '/dashboard';
          } else {
            alert('Error al importar algunos datos: ' + error.message);
          }
        } catch (err) {
          alert('Error al leer el archivo JSON.');
        }
      };

      reader.readAsText(file);
    });
  }

  initSettingsPage();
  document.addEventListener('astro:page-load', initSettingsPage);
</script>